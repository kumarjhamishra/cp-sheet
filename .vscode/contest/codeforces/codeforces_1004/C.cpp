#include <bits/stdc++.h>
using namespace std;

void solve(){
    int n;
    cin >> n;

    // basically in at max 9 times we can convert the last digit to 7 
    // we can see the 9's table afer adding 9 the last digit decrements by 1

    // so the question to have a digit 7 in <= 9 steps
    
}

int main(){
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);

    int t;
    cin >> t;
    while(t--){
        solve();
    }

    return 0;
}